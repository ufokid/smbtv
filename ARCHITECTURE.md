# 🏗️ SMBTV - Arquitetura Modernizada

## Estrutura do Projeto

```
smbtv/
│
├── 📁 app/
│   ├── 📁 src/main/
│   │   ├── 📁 java/com/smbtv/
│   │   │   ├── 📁 delegate/
│   │   │   │   ├── ApplicationDelegate.java
│   │   │   │   ├── SMBConfigDelegate.java
│   │   │   │   ├── SMBServerDelegate.java
│   │   │   │   ├── SMBShareDelegate.java
│   │   │   │   └── 📁 base/
│   │   │   │       ├── DAOBase.java
│   │   │   │       ├── DatabaseHandler.java
│   │   │   │       └── ...
│   │   │   ├── 📁 model/
│   │   │   │   ├── ServerInfo.java
│   │   │   │   ├── SMBConfig.java
│   │   │   │   └── SMBShare.java
│   │   │   ├── 📁 service/
│   │   │   │   └── SMBTVService.java
│   │   │   ├── 📁 ui/
│   │   │   │   ├── 📁 activity/
│   │   │   │   │   ├── MainActivity.java ✅ MODERNIZADO
│   │   │   │   │   ├── ShareActivity.java
│   │   │   │   │   ├── SettingsActivity.java
│   │   │   │   │   └── ...
│   │   │   │   ├── 📁 fragment/
│   │   │   │   │   ├── MainFragment.java
│   │   │   │   │   ├── ModernFragmentExample.java ✅ NOVO
│   │   │   │   │   └── ...
│   │   │   │   └── 📁 components/
│   │   │   │       └── ...
│   │   │   └── 📁 util/ ✅ NOVO
│   │   │       ├── PreferencesManager.java
│   │   │       └── Logger.java
│   │   │
│   │   ├── 📁 res/
│   │   │   ├── 📁 drawable/
│   │   │   ├── 📁 layout/
│   │   │   ├── 📁 mipmap-*/
│   │   │   ├── 📁 values/
│   │   │   └── 📁 xml/ ✅ NOVO
│   │   │       ├── backup_rules.xml
│   │   │       ├── data_extraction_rules.xml
│   │   │       ├── db_query_config.xml
│   │   │       └── ...
│   │   │
│   │   └── AndroidManifest.xml ✅ MODERNIZADO
│   │
│   ├── build.gradle ✅ MODERNIZADO
│   └── proguard-rules.pro
│
├── 📁 gradle/
│   └── 📁 wrapper/
│       └── gradle-wrapper.properties ✅ ATUALIZADO (8.4)
│
├── build.gradle ✅ MODERNIZADO
├── gradle.properties ✅ MODERNIZADO
├── gradlew ✅ ATUALIZADO
├── gradlew.bat ✅ ATUALIZADO
│
├── 📄 README.md
├── 📄 LICENSE.md
├── 📄 settings.gradle
│
└── 📚 DOCUMENTAÇÃO ✅ NOVA
    ├── MODERNIZATION.md - Detalhes técnicos
    ├── COMPILATION_GUIDE.md - Guia de compilação
    ├── MIGRATION_CHECKLIST.md - Próximos passos
    ├── MODERNIZATION_REPORT.md - Relatório completo
    └── ARCHITECTURE.md - Este arquivo
```

---

## Stack Tecnológico

### 🏗️ Build & Deployment
```
┌─────────────────────────────────────────┐
│     Android Gradle Plugin 8.1.2         │
│     (AGP - Gradle 8.4)                  │
└────────────────┬────────────────────────┘
                 │
        ┌────────┴────────┐
        ▼                 ▼
   ✅ D8/R8         ✅ ProGuard
   (Desugaring)    (Optimization)
        │                 │
        └────────┬────────┘
                 ▼
    ┌──────────────────────────┐
    │  APK/AAB Generation      │
    │  (Debug & Release)       │
    └──────────────────────────┘
```

### 📚 Dependencies Layer

```
┌───────────────────────────────────────────────────────┐
│           Application Code (Java 11)                  │
├───────────────────────────────────────────────────────┤
│                                                       │
│  ┌─────────────────┐  ┌──────────────────┐           │
│  │   UI Layer      │  │ Service Layer    │           │
│  │ (Activities,    │  │ (SMBTVService)   │           │
│  │  Fragments)     │  │                  │           │
│  └────────┬────────┘  └──────┬───────────┘           │
│           │                   │                       │
│           └───────────┬───────┘                       │
│                       ▼                               │
│     ┌─────────────────────────────────┐               │
│     │  Business Logic & Data Access   │               │
│     │  (Delegates, DAO, Repository)   │               │
│     └────────────┬────────────────────┘               │
│                  ▼                                    │
│     ┌─────────────────────────────────┐               │
│     │   Database & Config Layer       │               │
│     │   (SQLite, SharedPreferences)   │               │
│     └────────────┬────────────────────┘               │
├───────────────────────────────────────────────────────┤
│                   AndroidX Libraries                  │
├───────────────────────────────────────────────────────┤
│   AppCompat │ Leanback │ Fragment │ Lifecycle       │
├───────────────────────────────────────────────────────┤
│                Android Framework                      │
├───────────────────────────────────────────────────────┤
│    Android 5.0+ (API 21) to Android 14 (API 34)      │
└───────────────────────────────────────────────────────┘

External Libraries (via Maven):
├── JLAN (alfresco-jlan.jar)
├── Cryptix (cryptix-jce-provider.jar)
├── Material Design 3
├── Apache Commons Lang
└── FilePicker (NonSenseApps)
```

---

## Fluxo de Compilação Modernizado

```
┌──────────────────────┐
│   Source Code        │
│  (Java 11 + XML)     │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  Gradle Task Graph   │
│  (Gradle 8.4)        │
└──────────┬───────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌─────────┐  ┌──────────────┐
│ Compile │  │ Process      │
│ (Java)  │  │ Resources    │
└────┬────┘  └──────┬───────┘
     │              │
     └──────┬───────┘
            ▼
    ┌──────────────────┐
    │  Merge Resources │
    │  & Manifests     │
    └────────┬─────────┘
             │
             ▼
    ┌──────────────────┐
    │  D8 Desugaring   │
    │  (Java 8+ → 7)   │
    └────────┬─────────┘
             │
             ▼
    ┌──────────────────┐
    │  ProGuard/R8     │
    │  (Release only)  │
    └────────┬─────────┘
             │
             ▼
    ┌──────────────────┐
    │  Package APK     │
    │  (Sign & Align)  │
    └────────┬─────────┘
             │
             ▼
    ┌──────────────────┐
    │  Output APK/AAB  │
    │  Release Ready   │
    └──────────────────┘
```

---

## Componentes Principais - Antes vs Depois

### Activity Architecture

```
ANTES (2015)
┌──────────────────────────┐
│ Activity (extends)       │
│                          │
│ - Manual findViewById()   │
│ - GoogleApiClient (*)    │
│ - No ViewBinding         │
│ - setContentView()       │
└──────────────────────────┘

DEPOIS (2026)
┌──────────────────────────┐
│ AppCompatActivity        │
│                          │
│ + Auto ViewBinding       │
│ + Modern lifecycle       │
│ + Material Design 3      │
│ + Type-safe views       │
│ - No GoogleApiClient     │
└──────────────────────────┘
```

### Service Architecture

```
┌──────────────────────────────────┐
│    SMBTVService                  │
│  (SMB Server Implementation)      │
│                                  │
│  ├── JLAN Library                │
│  │   (Samba Protocol Stack)       │
│  │                               │
│  ├── SMBAuthenticator            │
│  ├── PersistentServerConfig      │
│  └── ServiceController           │
│                                  │
│  Status: Ready for Migration     │
│  to WorkManager (Optional)       │
└──────────────────────────────────┘
```

---

## Database & Persistence

```
┌─────────────────────────────────────┐
│   SharedPreferences                 │
│  (via PreferencesManager)           │
│  - Server config                    │
│  - User preferences                 │
└──────────────────┬──────────────────┘
                   │
                   ▼
┌─────────────────────────────────────┐
│   SQLite Database                   │
│  (via DatabaseHandler & DAO)        │
│  - Shares                           │
│  - Configuration                    │
│  - Query XML loading                │
└──────────────────┬──────────────────┘
                   │
                   ▼
┌─────────────────────────────────────┐
│   File System                       │
│  (External Storage)                 │
│  - Shared files (SMB)               │
└─────────────────────────────────────┘
```

---

## Gradle Configuration Flow

```
┌──────────────────────────────────────┐
│   gradle.properties                  │
│   (Global Gradle Config)             │
│                                      │
│  ✅ android.useAndroidX=true         │
│  ✅ android.enableJetifier=true      │
│  ✅ android.viewBindingEnabled=true  │
│  ✅ org.gradle.parallel=true         │
│  ✅ JVM Arguments optimized          │
└──────────────┬───────────────────────┘
               │
       ┌───────┴───────┐
       │               │
       ▼               ▼
    Root         App Module
    build.gradle build.gradle
    │             │
    ├─ AGP 8.1.2  ├─ compileSdk 34
    ├─ Gradle 8.4 ├─ targetSdk 34
    ├─ Plugins    ├─ Java 11
    └─ Repos      ├─ ViewBinding
                  └─ Dependencies
                     (All AndroidX)
```

---

## Dependency Resolution

```
┌────────────────────────────────────────┐
│     Maven Central / Google Repo        │
│     (Modern Package Sources)           │
└────────────────┬───────────────────────┘
                 │
    ┌────────────┼────────────┐
    ▼            ▼            ▼
 AndroidX   Material 3    Utilities
 Libraries   Design      Libraries
    │            │            │
    ├─ AppCompat ├─ Material  ├─ Commons
    ├─ Leanback  │  3.9.0     ├─ FilePicker
    ├─ Fragment  │            └─ Lifecycle
    └─ Lifecycle │
               (Removed:
                leanback-cards)
```

---

## Android Version Support

```
API Level Support:
┌─────────────────────────────────────┐
│  Target: API 34 (Android 14)   ✅   │
│  Compile: API 34 (Android 14)  ✅   │
│  Minimum: API 21 (Android 5.0) ✅   │
│                                     │
│  Coverage:                          │
│  ├─ Android 5.0 - 14 (Supported)   │
│  ├─ Android 15+ (Future ready)     │
│  └─ Android TV (All versions)      │
└─────────────────────────────────────┘
```

---

## Performance Improvements

```
Build Performance:
┌──────────────────────────────────────┐
│  Gradle 2.10 → 8.4                   │
│  • Parallel builds enabled           │
│  • Incremental compilation            │
│  • Faster dependency resolution       │
│  • Build cache support               │
│                                      │
│  Estimated: 3-5x faster builds      │
└──────────────────────────────────────┘

Runtime Performance:
┌──────────────────────────────────────┐
│  D8 Desugaring                       │
│  • Automatic Java 8+ features        │
│  • Optimized bytecode                │
│                                      │
│  R8 Obfuscation (Release)            │
│  • Code shrinking                    │
│  • Resource shrinking                │
│  • Aggressive optimization           │
└──────────────────────────────────────┘
```

---

## Security Enhancements

```
┌──────────────────────────────────────┐
│   Android 12+ Compliance             │
├──────────────────────────────────────┤
│                                      │
│  ✅ Data Extraction Rules            │
│  ✅ Backup Rules                     │
│  ✅ Modern Permissions               │
│    - READ_MEDIA_IMAGES              │
│    - READ_MEDIA_VIDEO               │
│    - READ_MEDIA_AUDIO               │
│  ✅ Activity exported attributes     │
│                                      │
└──────────────────────────────────────┘
```

---

## File Structure - Utilities

```
📁 util/
├── PreferencesManager.java
│   ├── saveString(key, value)
│   ├── getString(key, default)
│   ├── saveBoolean(key, value)
│   ├── getBoolean(key, default)
│   ├── saveInt(key, value)
│   ├── getInt(key, default)
│   ├── clear()
│   └── remove(key)
│
└── Logger.java
    ├── d(tag, message) - Debug
    ├── i(tag, message) - Info
    ├── w(tag, message) - Warning
    └── e(tag, message, tr) - Error
```

---

## Migration Path

```
Phase 1: Build System ✅ DONE
├── Gradle 8.4
├── AGP 8.1.2
└── AndroidX

Phase 2: Code Modernization ✅ STARTED
├── Activities → AppCompatActivity
├── View Binding
└── Material Design 3

Phase 3: Architecture 🔄 PLANNED
├── MVVM Pattern
├── LiveData/ViewModel
└── Repository Pattern

Phase 4: Advanced Features 📋 BACKLOG
├── Coroutines
├── WorkManager
├── Kotlin Migration
└── Jetpack Compose (Future)
```

---

**Última Atualização**: Janeiro 2026
**Versão**: 2.0 - Modernizada
**Status**: ✅ Pronto para desenvolvimento